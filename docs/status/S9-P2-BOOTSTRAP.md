# S9 P2 bootstrap marker

This temporary marker opens the P2 review cycle. The existing CI workflow applies the atomic API-contract bootstrap, restores its normal read-only definition, and removes the bootstrap script before the resulting implementation commit.
